find(): is used to check if the we get as input is presented or not in the files.

size(): checking the size of input.

front() and back() : input.front() is used to check the first character and input.back() is used to check the last character .

push_back(): it is a method used to add elements to the end of a  vector. it is push the input in word to print accordingly . 

vector<string>: results is declared as a vector<string> to store a collection of strings, Using a vector<string> allows for flexibility in storing and accessing strings 